import {Meta, Story, Preview} from '@storybook/addon-docs/blocks';
import DefaultButton from '../DefaultButton';

<Meta title="MDX|Button" component={DefaultButton} />

# Button

With `MDX` we can define a story for `DefaultButton` right in the middle of our
markdown documentation.

<Preview>
  <Story name="all buttons">
    <DefaultButton text="Button" bg="primary" onClick={() => 'clicked'} />
  </Story>
</Preview>
